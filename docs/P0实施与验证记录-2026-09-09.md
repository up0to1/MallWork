# P0 实施与验证记录

日期：2026-09-09。本轮在已有新版前端和 AG-UI 首版基础上，继续实施交易可靠性、队列和共享配额、Trace 接入、正式评测隔离。没有提交 Git、部署远端或接入支付物流。

## 已交付

| 工作 | 实现与证据 |
|---|---|
| 交易确认与原子库存 | 模型/详情表单只准备；前端明确决议；确认、操作幂等、订单与库存同一 SQLite 事务；真实商品库存投影；模型每轮恢复精简交易事实。见[专项记录](交易确认与库存验证-2026-09-09.md) |
| 前端交易体验 | 规格与收货表单、商品金额确认、批准/拒绝、取消确认、失败刷新；已结束卡片收起明细；刷新自动恢复当前本机会话并读取服务端确认状态 |
| 队列可靠性 | 双流 reclaim、持久终态去重、消息/task/session 租约、续期防误抢、失租取消、不 ACK 失租消息；API/worker 共用会话锁；request_id 绑定请求内容；等待结果只读目标 task 终态。见[队列验证](队列重投验证-2026-09-09.md) |
| 多实例模型配额 | Redis 原子并发名额与调用起点间隔、长流续租、失租取消、Redis 故障拒绝绕过；未读首分片就取消/关闭也释放实际 HTTP 连接。见[共享配额验证](共享网关配额验证-2026-09-09.md) |
| 端到端 Trace | W3C 上下文贯穿 API/队列/worker/Agent/模型/工具；响应头/事件/日志关联；OTLP 导出前过滤正文、地址及异常细节；Compose 与依赖同步。见[Trace 验证](Langfuse与端到端Trace.md) |
| 评测独立执行 | 三 runner 支持 dev/release/all、dry-run、运行清单、逐条证据和版本指纹；实际降级仍阻断 online-main；服务端启动源码指纹可核对。见[评测说明](正式评测选集与证据清单.md) |
| 就绪检查 | `/health` 暴露交易库和启动时 app 源码指纹；关键数据库或已配置 Redis 故障返回 503，不再以进程活着代替就绪 |

## 实际运行的验证

- 完整后端回归：**616 passed，52.49 秒**，无跳过。使用隔离编译的官方 Redis 7.4.8，测试自行创建本地 Unix socket 进程，不连接或清空用户已有 Redis。
- 前端：**60 passed**，`npm run build` 通过。生产构建主应用约 49 kB，SDK 和 UI 依赖独立分块。
- `git diff --check` 通过。
- 真实 qwen3-max + AG-UI 搜索复验通过：3 张商品卡，首协议事件 83 ms、首文本约 4.73 秒、总计约 11.09 秒，标准生命周期/工具参数 JSON/最终状态均正确。证据：[AG-UI 报告](../eval/verification/ag-ui-trade-batch-20260909.json)。
- 浏览器使用独立库完成表单准备、批准、取消、模型准备、拒绝和刷新恢复：订单数始终不超过 1，库存 80→78→80；模型重启后查询同一会话也正确识别 CANCELLED。手机 390px 下内容宽 375px，无横向溢出、无控制台错误。证据：[交易报告](../eval/verification/trade-browser-20260909.json)。
- 被测服务 app 启动源码 hash 与当前应用源码 hash 匹配；该证据不等于依赖、数据卷或外部模型版本一致。
- 本地真实 OTLP HTTP 接收器验证父子 Span、认证/路径、SSE 生命周期和脱敏；测试模型回调使用本地夹具，不冒充真实 Langfuse 验收。
- 正式 release dry-run 实际选择商品/知识/Agent **45/15/30** 条，状态为 NOT_RUN；关键词 dev/release 分开实际执行，既有 all 150 条批准基线复跑 PASS。证据位于 [评测证据目录](../eval/verification/split-20260909/final/)。

可复现检查：

```bash
GLOBEX_REDIS_SERVER_BIN=/path/to/redis-server .venv/bin/python -m pytest -q
cd frontend
npm test
npm run build
```

当前运行页面：[新版应用](http://127.0.0.1:5174/)，API 为 `127.0.0.1:8000`。交易浏览器验收使用 `5175 → 8001` 和 `/tmp/globex-trade-e2e-20260909-1219`，没有向日常库写入测试订单。

## 尚未完成的验收与后续优先级

1. **真实 Langfuse 看板**：本地配置/凭据尚未提供，已向用户询问配置位置。本机 collector 只证明协议、关联和脱敏，不能代替真实实例验收。
2. **正式在线发布门禁**：真实重排服务仍返回未找到模型服务，当前检索为 embedding_only。没有把降级结果记作 online-main 通过。还需为正式 Agent 交易场景加入显式确认动作步骤，再在隔离评测环境生成三份在线报告。
3. **完整恢复语义**：目前持久确认与终态可查；SSE 事件游标重放、跨进程会话持久 fencing/CAS 尚未完成。队列 Stream/终态记录的归档清理与实际故障转移验收仍需单独建设。
4. **身份边界**：本机 buyer_id 是客户端声明；归属校验已实现，但生产登录鉴权及会话/WS 权限绑定未完成。订单仍是未支付、未发货的项目意向单。
5. 随后按[优先级计划](待实现设计与实施计划-2026-09-09.md)推进上下文预算兜底、Hybrid 召回与索引更新、指标/Bad Case；Prompt A/B、按需 Skill、策略记忆仍在后续批次。

SQLite 事务已验证同机多连接/多进程；没有声明其他数据库或 Redis Cluster/故障转移等价保证。共享模型配额当前按主模型 scope 包含 retry/fallback，精确到每个上游 attempt/实际回退模型的配额归属仍保留在后续改进中。
