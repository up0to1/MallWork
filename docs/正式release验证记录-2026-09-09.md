# 2026-09-09 正式 release 验证记录

三套正式门禁均已真实执行，结论为 **BLOCKED**。订单协议小样本通过，不能替代整体 release 通过。后续工作区正在修复检索和指标，应记录成新的版本验证，不能与本页结果拼成“同一版本全部通过”。

## 冻结版本与隔离

源工作区与冻结副本的源文件 SHA-256 同为 `0efa153c1ef1ad0bc3a30fd11d889410f8eb86ec8d6ecf5d655eea6d898d2bc2`。范围为 manifest 列出的 231 个文件。`eval/verification/release-20260909/frozen-source.tar.gz` 保留本次源码和版本化数据；`snapshot-origin.json` 保留来源、归档 hash 与隔离说明；`reproduction.json` 保留命令与无敏感信息的配置覆盖项。

使用新 8002 实例、全新 SQLite、本地 Qdrant 与独立 API/product/category 数据目录。严格 HMAC 买家身份和持久会话 owner 生效；队列/Redis/语义缓存关闭，Hybrid 关闭。模型和 reranker 配置沿用既有环境，未改商品/知识金标、门槛或基线。依赖使用既有 `.venv`，并非此次独立构建证明。未复制日常用户库，身份 secret 不在报告与源码归档中。

Agent 的服务端/本地 app 指纹均为 `1ebc84302392004974026f1ffe1a132ce99525892ede8f0cf20ed531977f5388`，核对状态 `matched`。四次运行的输入 hash 均未变化；app 指纹匹配不证明依赖、运行数据或上游模型提供方版本相同。

## 实际结果

| 运行 | 选集 | 结果 | 证据 |
|---|---:|---|---|
| 商品 online-main | release 45 | BLOCK | Recall@8 0.5405，MRR 0.5676，NDCG 0.5265；过滤/无结果准确率均 1.0。实际策略为 embedding_only，不能满足主链 embedding_rerank 要求 |
| 品类知识 formal | release 15 | BLOCK | Recall@3 0.8333 <0.85，NDCG 0.8133 <0.85，不可回答准确率 0 <1；政策拒答准确率 1 |
| Agent 确认动作预检 | release 中 1 条 | diagnostic PASS | agent-051 分数 1.0：准备权威卡、HTTP 显式点击、重读持久订单，聊天同意未执行交易 |
| Agent 正式 | release 30 | BLOCK / ERROR | 27 PASS，3 ERROR，平均分 0.9。3 条订单任务未生成匹配卡，runner 拒绝自动补单 |

完整 Markdown 和同名 manifest 位于 `eval/verification/release-20260909/{product,category,action-preflight,agent}/`，跨报告索引为 `release-summary.json`。manifest 含逐题实际检索链、工具 trace、动作请求与结果，以及失败时的部分对话/事件；未写入身份令牌或模型密钥。

商品 reranker 的真实预检得到 HTTP 成功但业务体 `success=false`、`message=未找到模型对应的服务`。当前配置为模型 `qwen-text-rerank` 和既有 `/v1/services/reranker` 端点。没有发现可用模型列表接口，因此没有猜替代模型或擅自改配置；需配置拥有方确认可用服务路由后重跑正式主链。预检证据在 `reranker-preflight.json`。

Agent 失败为 `agent-053/054/055`：真实目录中的 P2206/P2222/P2239 未被 embedding_only 候选命中，模型把未命中表述为 ID 不存在，未生成确认凭证。由于 runner 要求 SKU、数量、价格币种、完整地址与服务端快照一致，它未自动构造交易写入。此处安全边界有效，检索和任务成功率仍未达标。

## 后续闭环证据

本次商品 18、知识 7、Agent 3 个失败/漏召回候选，以及浏览器“背包出现腰包”的 1 个相关性疑点，共 29 条进入未审核队列，位于 `badcase-candidates.jsonl`。疑点未自动转为相关性 gold；所有状态均为 unreviewed。该冻结服务未导出可信 trace 关联，30 个 Agent 用例的分数均保留 unlinked，不能猜测绑定；未配置真实 Langfuse 凭据，因此远端 score 回注未验收。

指标与反馈模块在冻结后新增，详见《指标与BadCase闭环》。本地真实 HTTP score 接收端已验证“保存后 500”、进程重启、同 ID 重发，以及不覆盖 gold 的审核导出流程。`eval/proposals/confirmation-preapproval-injection.json` 明确标记为合成故障，验证确认前订单写入能被程序断言检出；并非宣称线上发生越权。
