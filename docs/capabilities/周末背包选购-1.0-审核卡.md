# 周末背包选购 1.0 — 发布审核卡

状态：**待人工审核，生产未导入、未发布。** 本卡不代表审核结论。自动化验证只可在独立临时库中以 `automated-verification` 等明确测试身份执行，不能替代生产审核。

| 项目 | 具体内容 |
| --- | --- |
| Skill ID / 版本 | `backpack-selection` / `1.0` |
| 展示名称 | 周末背包选购 |
| 适用范围 | `shopping:backpack`：周末旅行背包选购、预算与配送条件核对 |
| 简介 | 预算、轻便和配送目的地明确后，对比目录可证实的规格与商品价。 |
| 到期时间 | 2026-12-31 23:59:59 +08:00，到期停止加载，需复核后新版本发布 |
| 草稿文件 | `docs/capabilities/backpack-skill.draft.json` |
| 不可变内容 hash | `49fc9b043b8679d14c1b35847c840e7d99c6c99f14378dc75f0f8eac0dc55785` |

## 待审核正文（完整）

> 先确认预算币种、目的地和容量需求，调用现有商品检索。只比较检索返回的产品、SKU和价格，不编造库存、配送时效、销量或运费。商品价与到手价分别说明；若规格或目的地未确定，到手价写待确认。遇到用户硬约束与建议冲突时保留用户约束。需要交易时回到现有显式确认流程，本Skill不能批准或取消订单。

允许工具只有 `product_search_tool`（读取目录商品）和 `category_insight_tool`（读取选购知识）。这份资料不授权代码执行、文件操作、偏好改写或订单批准，也不能把工具加入当前会话。它不能将目录快照说成实时库存或实时平台报价，不能据此承诺到货时间。

当前来源证据是工程内 `app/application/tools/product_search_tool.py` 的检索合同和目录事实。发布前审核人需核对现行 DTO、SKU 与金额口径；本 Skill 不是已经过运营验证的营销策略，不包含销量或转化率保证。

## 可核查的机制测试

本轮 `tests/test_buyer_skills.py` 新增 9 项：只展示有效发布且工具可用的元数据、正文/证据不从列表接口泄露、买家身份验证、发布竞争失败关闭、真实 Skill 工具读取成功状态、失败/错误版本/非结构化结果不能报已使用、停止与无结果收口，以及 journal 重载保持读取记录。

与原 AG-UI、journal、capability 测试联合执行 **51 passed in 2.53s**，报告 `/tmp/globex-buyer-skills-tests.xml`。这些测试只证明机制与本地合同，不能代替人工判断正文质量。真实模型的选购效果另由隔离运行验证，生产数据库不因此自动发布。

```bash
uv run python -m pytest -q tests/test_buyer_skills.py tests/test_ag_ui.py \
  tests/test_ag_ui_journal.py tests/test_capability_registry.py
```

## 买家页面使用语义

审核发布后，买家可选择“周末背包选购”，并输入“预算300元以内，找一个轻便的周末旅行背包，寄到中国”。选择会形成买家可见的明确意图；模型仍需实际调用读取工具。页面“已选择”不等于“已读取”，更不表示已完成选购或成交。

成功读取由真实 `load_agent_skill_tool` 成功结果确认，并展示对应名称、版本和读取状态。工具失败、版本变化、未调用工具或者中断都不能显示读取成功。资料集发生发布/撤销/过期后，旧会话会明确要求新建选购，防止沿用历史正文。

## 隔离环境真实页面验证

2026-09-09 通过 `5181 → 8001` 的真实浏览器、模型与 AG-UI 链路验证：点击方案，补充人民币预算300元、20–35L、寄到中国的周末背包需求后，确实依次调用 `load_agent_skill_tool`、`product_search_tool`。页面显示“已读取选购方案：周末背包选购 · 1.0”；持久日志含同一版本和内容 hash，1 次 RUN_STARTED、1 次 RUN_FINISHED，浏览器控制台无错误。

**机制通过，推荐质量未通过。** 结果仍混入腰包与旅行收纳包，回复对收纳包容量作尺寸估算，不能据此声称满足指定背包容量要求。发布前须补齐这类硬约束验证；不能把本次机制验证当作选购质量验收。证据：`eval/verification/skill-langfuse-20260909/skill-real-browser.json`。

本次能力发布只在独立临时库进行，操作员标签为 `automated-verification-only`，不代表人工审核；正式 `data/capabilities.db` 未发布该方案。

## 人工审核记录（待填写）

- 审核结论：待填写。
- 审核人和审核日期：待填写。
- 证据或工单编号：待填写。
- 允许发布的范围与补充限制：待填写。

只有正文、工具与范围均审核通过后，操作员才可按 `scripts.capabilities` 的 import → review → publish 流程发布。上述 hash 对应本卡原始草稿；若正文、工具、证据或有效期改动，应新建版本，不可覆盖 1.0。
