# 全计划实施与验证记录

日期：2026-09-09。范围为本工程及已批准的 12 个工作包，包含前端重设计与 AG-UI。本轮在原 P0 交付基础上补齐剩余机制、运行真实评测并保留失败证据。代码实现完成与正式发版达标分开记录，不以单元测试代替线上质量门禁。

## 当前交付摘要（个人工作区与刷新修复后）

- 买家可在“我的 Skill”创建、编辑、删除个人方案，服务端按买家保存不可变修订，输入 `/` 选择或由 Agent 按需加载；“长期偏好”支持页面管理及 Agent 记住、原子修改、精确删除。每轮读取最新偏好和个人目录，详见[个人工作区记录](买家自写Skill与长期记忆-2026-09-09.md)。
- 刷新恢复已补齐：正文缓存缺失或损坏仍按会话指针从服务端恢复；没有指针时恢复该买家最新会话；主动新选购保持空白。导航位置刷新保留，买家切换不继承旧缓存。已在用户原标签页原地刷新，核对完整对话和“喜欢小香风连衣裙”偏好仍可读取，未改删真实用户记录，见[刷新修复](刷新恢复修复-2026-09-09.md)。
- 最新后端全量证据为 **931 passed、0 skipped**，随后工具合同纳入个人 Skill 存储实现的相关 **72 项**复验通过；最新前端为刷新修复阶段 **96 passed、生产构建通过**。931 与 96 来自不同阶段，不声称同一次执行。后端及真实模型证据位于 `eval/verification/buyer-workspace-20260909/`；前端刷新实测见上述专项记录。
- 真实偏好修改 Langfuse Trace `548bd69c08cc1a19ecd32792a465ebb8` 远端 API 回查 VERIFIED，API 1、Agent 1、模型 2、工具 1；费用未知。此前商品 Trace 与评分验证保留，worker 远端和网页 UI 未新增验收。
- [README](../README.md) 已按当前实现重整：Codex 协助启动、本机/Compose 环境、配置优先级、双执行路径、数据位置、身份与刷新、页面使用、验证与排错。此次文档整理未改业务代码或运行配置，也未重跑正式质量门禁。 文档验证：6 份更新文档的本地链接全部存在，README 的 9 个 Shell 代码块通过语法检查；uv frozen dry-run、Uvicorn/Vite 命令帮助、使用占位凭据的 Compose config --quiet 及 git diff --check 通过。没有安装/卸载依赖、重启服务或改动数据。

剩余限制：在线精排服务与独立 release 质量达标、真实登录/跨设备身份、worker Langfuse 远端验收、Hybrid/Prompt A/B/策略的收益验证。既有 release BLOCK 不因上述功能和文档交付而改写。

## 前阶段：结构化 Skill 选择与远端接入

2026-09-09 后续交付已完成结构化 Skill 选择和真实 Langfuse API 验收：

- 本机 `data/capabilities.db` 已启用“选购需求梳理”1.0，读取版本/hash 与三个真实模型用例一致；独立自动化语义审读 3 项通过、0 项阻断。审核 actor 为 `codex-local-functional-review`，发布 actor 为 `codex-user-authorized-local-demo`，仅限用户授权的本机演示，不声称人工业务审核。背包推荐示例仍未发布。
- 真实商品业务 Trace `2e86df81c08bccf146fe559fb5b2a71e` 在 Langfuse 远端回查为 `VERIFIED`：API 1、Agent 1、模型 2、工具 1，父子链完整、评分关联正确；输入/输出 Token 为 13,969/184，费用未知保留 `null`。本轮未登录网页 UI，也未验证 worker 远端链路；worker 保留既有本机验证。
- 本次源码变化后的后端全量回归 **918 passed、0 failed、0 skipped（87.81 秒）**，前端 **85 项测试通过、production build 成功**。后端报告为 `backend-full-tests.xml` / `backend-full-tests.log`，不沿用前阶段的 835 项数字。

当前证据位于 `eval/verification/slash-skill-langfuse-20260909/` 的 `local-skill-publication.json`、`semantic-review.json`、`trace-and-score-remote.json`。这些新增事实不覆盖此前商品、知识或 Agent 的 release BLOCK，也不改变其冻结源码和数据边界。

该阶段 API 8000 已完成重启并返回 `/health` 200，OTel 已启用；运行代码指纹为 `8079d781aa73d6c1336952bde1bc354ebc9ebce1117290bd7f0ce6740d367b8a`。后续页面 5174 的真实验收以个人工作区及刷新修复记录为准，该指纹仅属于本阶段。

## 前阶段工作区验收（本次 Skill/远端接入更新前）

**后端 835 passed、0 failed、0 skipped，前端 65 passed，production build 通过。** 后端包含真实 Redis、交易持久化、预算与取消、上下文回查、能力版本竞争、20 轮各 6 个 SQLite 实例并发初始化和实际 TCP 断线恢复。仅有一条 Starlette/httpx 开发测试接口迁移提醒，不影响本轮结果。

该阶段运行服务与工作区 `app_source_sha256` 一致：`74cc089949efcf544c5a7d87af444152b14188befd3058d9fac3e251b2bfbb04`。这是该阶段指纹，不是后续修改后的当前源码指纹。证据为 `eval/verification/final-20260909/accepted-workspace-junit.xml`、`accepted-workspace-pytest.log`、`accepted-runtime.json` 和 `delivery-validation.json`。

本地页面已更新并打开：<http://127.0.0.1:5174/>。最终浏览器确认两次独立商品查询都保留卡片，顺序是 P1001-S2（¥199）、P1003-S1（¥129）；持久候选一致。API 重启后新页面恢复两张卡片，无控制台错误。Nginx 最终镜像重新构建通过，代理所连 API 的源码指纹也核对一致。

## 12 个工作包交付

| 工作包 | 已交付 | 验证与边界 |
|---|---|---|
| P0 交易可靠性 | 持久确认资源、HTTP 明确决议、原子订单/库存/操作台账、幂等恢复 | 真实 SQLite、浏览器创建/取消及未确认拒执行；不是支付或真实平台订单系统 |
| P0 队列与会话 | 双流 pending 回收、worker 租约、会话 fencing/revision CAS、买家归属、全局配额；终态先归档后安全清理 | 真实 Redis 含 AOF + SIGKILL 重启；归档不裁掉 pending 或未投递任务，幂等墓碑保留 |
| P0 端到端 Trace | API→队列→worker→Agent→工具上下文关联、OTLP 脱敏导出 | 本机真实 OTLP 验证；真实 Langfuse 商品业务 5 个 span 与评分 API 回查通过，网页 UI 与 worker 远端本轮未验收 |
| P0 正式门禁 | dev/release 独立选集、源码/Prompt/数据 hash、运行策略、服务版本核对、完整失败报告 | 原始冻结三套完整 release 已运行；商品/知识/Agent 门禁各自记录，未降低阈值 |
| P0 前端与 AG-UI | 暖白墨绿页面、留白、流式状态、商品卡/规格/比较、事务确认、停止；持久游标恢复与静态 Nginx 镜像 | 真实模型/商品/HTTP/SSE/浏览器跨页恢复验证；API 崩溃后明确中断，不能续跑模型执行栈 |
| P1 上下文与预算 | 完整工具证据和决策投影分离、冷证据引用回查、两阶段压缩、偏好 revision、候选序号持久化、并行预算预留与规则兜底 | 回归验证预算耗尽零新增调用、失败重试结算、撤回/重启/旧 fence；投影字符体积降低 23.04%，不冒称 Token 收益 |
| P1 检索与索引 | BM25+向量并行、RRF、去重与补召回；精确 ID 直查；知识内容 hash 更新/删除及文档主题覆盖 | Hybrid 默认关闭，150 条固定词法对比未证明收益；新知识开发集 35 条通过，正式复验独立留证 |
| P1 指标与分数 | 请求/模型时延、Token、工具失败、积压、低样本告警；受 HMAC/操作员名单保护的指标接口；分数 outbox | 本地 HTTP 失败重发幂等验证，真实 Langfuse 评分按 Trace 主体等字段回查通过；远端费用缺失仍为 null |
| P1 Bad Case 闭环 | 证据导入、稳定 ID、人工审核、候选导出与回归关联 CLI | 正式失败与浏览器发现导入未审核候选，未自动改 gold；合成样例验证审核流程 |
| P2 Prompt 版本与 A/B | 不可变正文+工具合同、买家稳定分桶、会话固定版本、指标门禁、回滚/撤销 | 本地机制与阻断测试；未发布候选，无成功率/成本收益结论 |
| P2 Skill 渐进加载 | 常驻元数据、指定版本按需读取正文、结构化选择时服务端确定预读、审核发布、证据与撤销 | 本机已启用需求梳理 1.0，3 个真实用例自动审读通过；背包未发布，不新增工具或交易授权，未证明业务收益 |
| P2 审核策略记忆 | 独立于买家偏好的版本库、适用范围、证据、关键词、期限与撤销；会话绑定有效版本摘要 | 不自动晋升经验，不覆盖交易权限/用户约束；有效集合变化使旧会话明确停止，防旧正文继续生效 |

## 真实评测结论

原始冻结源码、可复现命令、报告及输入 hash 位于 `eval/verification/release-20260909/`，详见 [正式 release 验证记录](正式release验证记录-2026-09-09.md)。

- 商品 release：45 条完整运行，实际 `embedding_only`，精排网关返回“未找到模型对应的服务”。Recall 0.540、MRR 0.568、NDCG 0.526，硬约束/空结果检查为 1.0，最终 BLOCK。
- 知识 release：15 条完整运行，原版 Recall 0.833、NDCG 0.813、不可回答准确率 0，最终 BLOCK。已针对开发集定位的文档重复、主题漏召回与证据范围问题修复；开发集通过不代替 release。
- Agent release：30 条完整运行，27 PASS、3 ERROR，最终 BLOCK。三个失败是指定 ID 未进入向量候选导致无法创建对应确认资源；评测没有绕过确认继续交易。已实现权威目录 ID/SKU 直查并回归。

第二份冻结快照 `3ef9cb7200d6a361afd1d1188296544e6b70ef387fbc8a12f167e4a0d2e1f300`（252 个文件）结果位于 `eval/verification/final-20260909/`：

- 全后端 **798 passed、0 skipped**（真实 Redis）；商品完整 45 条仍因精排不可用 BLOCK。
- 知识完整 15 条的 Recall/MRR/NDCG 为 1.0、政策拒答为 1.0，但三个不可回答场景仍未拒答，保持 BLOCK。针对它们暴露的“未知平台承诺、缺地址时效、无来源传言”补充了通用证据边界与回归；该修复使用了 release 发现，不能把同一数据集再当从未见过的独立验收证据。
- Agent `053/054/055` 三项修复诊断各 PASS 1.0，实际 HTTP 确认及持久订单通过，输入 hash 不变且服务版本匹配。053/055 观测到 `exact_id_lookup`，054 本次向量降级召回正确命中。三次真实用量统计完整；货币成本未知保留 null。**不能与旧 27 条拼成新的完整 30 条 release PASS。**
- 冻结后的只读审查发现能力版本校验/读取间竞争，已将正文/摘要读取与 expected_digest 校验放进同一事务，新增三个确定性竞争测试通过。该修复和知识拒答末次修复在工作区重新跑完整回归，证据以 `workspace-final-*` 命名，与第二份快照严格区分。

部署侧还补了 Redis `appendonly yes / appendfsync always`，降低进程被杀后已接收队列任务丢失的风险；实际吞吐与磁盘故障/多主容灾仍需独立环境验收。此配置后于第二份快照，不追填进原评测 hash。

## 前端实际验收

前端 Docker 从干净 `npm ci` 构建时发现缺少 `@types/node`，已显式锁定开发依赖；Nginx 配置通过真实容器检查。隔离容器 5176 经同源代理到隔离 API 8001，真实 SSE 验证首事件约 223ms、首文约 3.21s，最终渲染两张真实目录商品卡（`eval/verification/nginx-agui-20260909.json`）。这是单次观测，不是性能 SLA。

浏览器在一轮运行中打开同源新标签页，恢复同一 run 后收到最终消息；下一轮按卡片顺序回查第二个商品得到 P1003 与两个真实 SKU；显式停止后服务端持久状态为 stopped，保留此前内容。证据见 `eval/verification/recovery-browser-20260909.json`。该中间版本出现正文自排与卡片序号不一致，已在后续 Prompt 中规范序号和 ID，不能将旧截图当最终 Prompt 的证明。

## 保持后置的项目

显式 Prompt Cache、自适应压缩阈值、自动语义判定、无人审核自进化、K8s/训练平台未加入本轮。它们在批准计划中属于有收益证据后再开展的项目。Hybrid、Prompt A/B 和策略候选未因机制测试通过而自动开启或发布；Skill 的本机需求梳理初始方案按本轮明确授权和功能验证单独启用，不据此宣称收益已被证明。

真实 Langfuse 项目配置阻塞已解除，商品业务 Trace 与评分远端 API 回查通过；网页 UI 和 worker 远端验证本轮仍未执行。在线精排验收仍需可用的 reranker 服务，该外部阻塞及原正式门禁 BLOCK 保留。真实密钥留在本机环境配置，不复制到报告或 Git。

## 专项记录

- [买家自写 Skill 与长期记忆](买家自写Skill与长期记忆-2026-09-09.md)
- [刷新恢复修复](刷新恢复修复-2026-09-09.md)

- [上下文与检索](上下文与检索验证-2026-09-09.md)
- [AG-UI 持久恢复](AG-UI持久运行与断线恢复-2026-09-09.md)
- [会话 fencing 与身份](会话持久Fencing与身份模式.md)
- [队列归档与重启](队列安全归档与Redis重启验证-2026-09-09.md)
- [Prompt 版本与人工发布](Prompt版本与人工发布.md)
- [指标与 Bad Case](指标与BadCase闭环.md)
- [Skill 与审核策略](Skill与审核策略版本管理-2026-09-09.md)

## 最后发现并修复的问题

- 并行/分次精确检索只保留最后一张卡：已使用共享的本轮候选投影，按用户明确多商品 ID 的顺序合并真实卡片，并在会话成功持久化后保存同一顺序。普通查询、新运行和空结果不盲目合并，未返回的商品不推断存在。
- SQLite 首次初始化偶发锁冲突：有限重试覆盖 WAL 与 DDL 全过程，耗尽并关闭 PRAGMA 游标，失败关闭连接，建表事务化。初始化短 busy timeout 与总期限限制等待，结构/损坏错误不重试；正常业务读写超时不变。
- Skill/策略版本绑定后的读取竞争：在读取正文和摘要的同一事务内核验有效版本摘要，防新正文被标成旧执行版本。

前阶段收尾时曾停止隔离评测 API 8001/8002/8003，保留日常 API 8000 与页面 5174；后续 Skill 验证又使用了隔离服务，该旧进程状态不作为当前运行清单。历史报告、冻结源码和静态镜像保留供复现。本轮 Langfuse 是向用户授权的远端观测项目导出/回查数据，不是把应用代码部署到远端。
