# 2026-09-09 修复版本冻结复验

这次验证对应源文件 SHA-256 `3ef9cb7200d6a361afd1d1188296544e6b70ef387fbc8a12f167e4a0d2e1f300`、252 个文件的冻结副本。完整证据在 `eval/verification/final-20260909/`，索引为 `verification-summary.json`，源码归档和 hash 见 `snapshot-origin.json`。API 使用新 8003、严格 HMAC 身份、新 SQLite 与独立本地 Qdrant；模型配置未改。

| 检查 | 实际结果 |
|---|---|
| 后端完整 pytest | 798 passed，0 skipped，89.76 秒；包含真实 Redis 官方二进制验证。唯一 warning 是 Starlette 的 httpx 迁移提醒 |
| 商品 online-main release 45 | BLOCK；实际 embedding_only，Recall@8 0.5405、MRR 0.5676、NDCG 0.5265；重排路由仍不可用 |
| 知识 formal release 15 | BLOCK；Recall/MRR/NDCG 均 1.0，但不可回答准确率 0/3，政策拒答 1.0 |
| Agent 修复诊断 | agent-053/054/055 各 PASS 1.0，均真实执行一次 HTTP 确认并重读持久订单 |

四类检查的作用不同：完整商品/知识 release 仍阻断；3 条 Agent 诊断只证明相应失败路径本次通过，不能与旧版本的 27 条 PASS 拼成新版本 30/30。旧完整 Agent 27 PASS/3 ERROR 报告保持不变。

3 条诊断的运行输入 hash 均稳定，服务端 app 指纹与 runner 本地匹配：`46c470537df6fe06f1c2d06d0a2daa7d1a4a88887b6efa4954270702c98b77a0`。053/055 的 trace 实际观察到 `exact_id_lookup`；054 本次用 `embedding_only` 命中。没有把三条全部归功于未观察到的路径。

首次真实服务指标闭环观察到每条两轮 `usage.summary`，模型 token 完整，美元成本仍 unknown：

| 诊断 | Agent 输入 token | Agent 输出 token | 对话+HTTP 动作耗时（不含 Judge） |
|---|---:|---:|---:|
| agent-053 | 35935 | 814 | 33.77 秒 |
| agent-054 | 28191 | 642 | 23.00 秒 |
| agent-055 | 44066 | 1004 | 34.40 秒 |

以上只统计埋点的 Agent LLM attempt，不包含 embedding/reranker/Judge 费用，不能称总成本；没有批准价目表所以 `cost_usd=null`。这些是 3 条诊断样本，不能作为完整 release 的成本/时延发布证明。

知识剩余失败题为 kb-043 未登记平台售后承诺、kb-044 缺地址的末端派送时间、kb-045 无来源网传免税额度。正式报告保存了错误返回的文档。工作区后续对这些已见失败的修复只能另作回归/诊断，不再称该集是独立未见验收。本次冻结后还发生了 capability 和部署配置修复，因此后端 798 项结果也不追溯代表那些后续变更；最终工作区需独立完成末次回归。
